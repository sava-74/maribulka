# 📌 Память проекта Maribulka

**ВАЖНО:** Основной файл памяти проекта перенесён в автоматическую память Claude Code.

## Расположение памяти

**Актуальный файл памяти для cloude:**

```

   C:\Users\sava\.claude\projects\d--GitHub-maribulka\memory\MEMORY.md
```

**Актуальный файл памяти для остальных:**
```

   "D:\GitHub\maribulka\memory"
```

Этот файл автоматически загружается в каждой сессии работы с Claude Code.

## Что содержит память:

1. **Критическая информация о сервере и деплое**
   - Структура серверов (локальная разработка + продакшн)
   - Vite proxy конфигурация
   - SSH подключение к BeGet
   - База данных

2. **Структура проекта**
   - Frontend (Vue 3 + TypeScript + Vite)
   - Backend (PHP API)
   - Ключевые файлы и компоненты

3. **Правила и паттерны**
   - Организация стилей (БЕЗ `<style>` блоков!)
   - Формат ID заказов
   - Цветовые схемы статусов
   - Мобильная адаптация (≤350px)

4. **История изменений**
   - Последние изменения (13-14.02.2026)
   - Исправления багов
   - Новые функции

5. **Эталоны и шаблоны**
   - Структура таблиц (TanStack Table БЕЗ чекбоксов)
   - CSS переменные (theme.css)
   - Модальные окна

---

## Для чего этот файл?

Этот файл (`Memory.md`) теперь служит только как **ссылка** на основной файл памяти в автоматической системе Claude Code.

**Не редактируй этот файл вручную!** Все обновления должны вноситься в:
```
C:\Users\sava\.claude\projects\d--GitHub-maribulka\memory\MEMORY.md
```

Claude Code автоматически загружает содержимое MEMORY.md при каждом запуске сессии.
