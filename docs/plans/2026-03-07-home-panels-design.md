# Home Page — 4 панели-заглушки

**Дата:** 2026-03-07
**Статус:** Approved

## Задача

Разместить в `Home.vue` 4 рабочие панели вертикально с текстом-заглушкой.

## Дизайн

- Корневой `<div>` — простая обёртка без классов, flex column, align-items: center
- 4 панели `.padGlass.padGlass-work` одна под другой, полная ширина (как padGlass-work)
- Текст-заглушка: «Панель 1», «Панель 2», «Панель 3», «Панель 4»
- Скрипт сохраняем как есть (импорты модалок не трогаем)
- Никаких новых CSS — только существующие классы из padGlass.css

## Реализация

Файл: `maribulka-vue/src/components/home/Home.vue`
Изменение: только `<template>` — добавить 4 блока `.padGlass.padGlass-work`
